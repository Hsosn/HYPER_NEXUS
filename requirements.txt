# ── Core server ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-multipart>=0.0.9
websockets>=12.0
python-dotenv>=1.0.0

# ── Secrets / encryption (required for at-rest API key storage) ──────────────
cryptography>=41.0.0

# ── HTTP / web ────────────────────────────────────────────────────────────
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── Documents ────────────────────────────────────────────────────────
python-docx>=1.1.0
python-pptx>=1.0.0

# ── Search ────────────────────────────────────────────────────────────────────
ddgs>=7.0.0

# ── File I/O ──────────────────────────────────────────────────────────────────
aiofiles>=23.2.0

# ── Embeddings / memory ───────────────────────────────────────────────────────
numpy>=1.26.0

# ── Browser automation ────────────────────────────────────────────────────────
# After installing run: playwright install chromium
playwright>=1.44.0

# ── v18 additions ─────────────────────────────────────────────────────────────
# OAuth / integrations
authlib>=1.3.0

# ── ML / AI (PyTorch + ecosystem) ───────────────────────────────────────────
# Core ML framework
sentence-transformers>=2.2.0
torch>=2.1.0
torchvision>=0.16.0

# Local image generation (diffusers + model)
diffusers>=0.38.0

# Audio processing
torchaudio>=2.1.0

# Text processing (torchtext deprecated, removed from PyPI for Python 3.14+)
# Install manually if needed: pip install torchtext

# Data science toolkit
scikit-learn>=1.3.0
pandas>=2.0.0

# Image processing (required by CV workbench and data augmentation)
Pillow>=10.0.0

# Model export and optimization
onnx>=1.15.0
onnxruntime>=1.16.0

# NLP / Transformers
transformers>=4.35.0,<5
tokenizers>=0.14.0

# Vision model helpers (required by Florence-2 local vision engine)
einops>=0.7.0
timm>=0.9.0

# Accelerate — optional, enables device placement optimizations
accelerate>=0.28.0

# Sentencepiece — optional, used by some tokenizers
sentencepiece>=0.2.0

# ── Voice / Speech ──────────────────────────────────────────────────────────
# Local STT (runs on CPU)
faster-whisper>=1.0.0

# Audio recording from microphone
sounddevice>=0.4.6

# Audio file I/O
soundfile>=0.12.1

# HuggingFace Hub (for voice model downloads)
huggingface_hub>=0.20.0

# TTS: KittenTTS (15M-80M params, ONNX CPU inference) — lightweight replacement for Kokoro
# Uses onnxruntime CPU inference — no GPU or C compiler needed.
kittentts>=0.1.3

# Protobuf — optional, used by some model configs
protobuf>=4.25.0

# Reinforcement learning environments
gymnasium>=0.29.0
stable-baselines3>=2.1.0

# ── Database (SQLite) ──────────────────────────────────────────────
aiosqlite>=0.20.0

# ── Task Queue (Celery + Redis) ────────────────────────────────────
celery[redis]>=5.3.0
redis>=5.0.0

# ── Media Processing (FFmpeg - must install separately) ────────────────
# FFmpeg is a system binary, NOT a Python package.
# Install manually:
#   macOS:  brew install ffmpeg
#   Linux: sudo apt install ffmpeg
#   Windows: choco install ffmpeg
#
# After install, these tools become available:
#   - ffmpeg_convert: Convert between video/audio formats
#   - ffmpeg_extract_audio: Extract audio from video
#   - ffmpeg_create_gif: Create animated GIF from video
#   - ffmpeg_merge_video: Join multiple videos
#   - ffmpeg_trim: Trim video to time range
